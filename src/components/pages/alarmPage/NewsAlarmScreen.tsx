import React from 'react';
import { View } from 'react-native';

import ScreenWrapper from '../../organisms/common/ScreenWrapper';

const NewsAlarmScreen = () => {
    return (
        <ScreenWrapper isPaddingHorizontal>
            <View></View>
        </ScreenWrapper>
    );
};

export default NewsAlarmScreen;
