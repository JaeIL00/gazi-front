import React from 'react';
import { View } from 'react-native';

import { ThreadItemTemplateProps } from '../../../types/types';

const ThreadItemTemplate = ({ post }: ThreadItemTemplateProps) => {
    return <View></View>;
};

export default ThreadItemTemplate;
